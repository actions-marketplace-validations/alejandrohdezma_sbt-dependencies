# An `<exclusions>` block on a BOM's `<dependencyManagement>` entry travels to every consumer that takes its
# version from the BOM (`*`), without any local annotation: the module keeps the excluded coordinate out of its
# graph, the consumer's own `exclude` entries are merged with the BOM's, and a `*:*` wildcard becomes intransitive.
> assertInheritedFromBom
> assertMergedWithOwnExclusions
> assertWildcardBecomesIntransitive
